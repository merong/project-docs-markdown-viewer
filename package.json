{
  "name": "project-docs-markdown-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Local markdown docs viewer with live refresh for current file",
  "type": "commonjs",
  "bin": {
    "mdview": "./src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "start:port": "node src/server.js --port 18094",
    "check": "node --check src/server.js && node --check src/file-indexer.js && node --check src/path-guard.js && node --check src/watch.js && node --check src/viewer-template.js && node --check src/public/viewer-app.js",
    "build:sea": "node scripts/build-sea.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "postject": "^1.0.0-alpha.6"
  }
}
